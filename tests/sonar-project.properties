sonar.projectKey=jehon-cryptomedic
sonar.projectName=Cryptomedic
#sonar.projectVersion=1.0

sonar.sourceEncoding=UTF-8

# Comma-separated paths to directories with sources (required)
sonar.sources=./
sonar.tests=test

sonar.exclusions=**/libs/**,**/vendor/**,**/unused/**
sonar.test.exclusions=libs
sonar.test.inclusions=test

#sonar.coverage.exclusions=libs

# Uncomment this line to analyse a project which is not a java project. 
# The value of the property must be the key of the language. 
sonar.language=php 
sonar.tests=test/

#sonar.php.tests.reportPath=tmp/phpunit.xml
sonar.php.coverage.reportPath=tmp/cache-test/coverage.xml
